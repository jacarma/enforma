{
  "name": "enforma-root",
  "private": true,
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r --filter './packages/*' build",
    "dev": "pnpm --filter demo dev"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
